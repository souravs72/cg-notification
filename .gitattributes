# =====================================================
# Strict file integrity — NO normalization
# =====================================================

# Everything is binary by default (no newline normalization)
* -text

# Explicit binary formats (documentation + safety)
*.jar   binary
*.class binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.pdf   binary
*.zip   binary
*.tar   binary
*.gz    binary

# Build outputs should not be normalized if ever tracked
target/** -text
