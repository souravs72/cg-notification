# Terraform state files (CRITICAL: never commit these - contain sensitive infrastructure state)
*.tfstate
*.tfstate.*
*.tfstate.backup

# Terraform plan files (never commit - may contain sensitive infra details)
tfplan
*.tfplan
*.tfplan.*

# Terraform provider directory
.terraform/
.terraform.lock.hcl

# Terraform CLI configuration
.terraformrc
terraform.rc

# Crash log files
crash.log
crash.*.log

# Local .tfvars files (may contain sensitive data)
*.tfvars
!terraform.tfvars.example
*.tfvars.json

# Override files (usually used for local overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json




